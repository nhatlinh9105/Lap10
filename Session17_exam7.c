#include <stdio.h>
#include<string.h>

void main()
{
    char compname[20] = "Microsoft";
    int len, ctr;* Determines the length of the string */
    for(ctr = 0; ctr < len; ctr++)
        printf("%c * ", compname[ctr]);
}
